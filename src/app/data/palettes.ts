/**
 * Default color palettes for the PixiJS project
 */

import type { BasicPalette } from '../../types';

export const defaultUIPalette: BasicPalette = {
  red: 0xFF0000,
  blue: 0x0000FF,
  green: 0x00FF00,
};
